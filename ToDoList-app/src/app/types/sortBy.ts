export enum SortBy {
  date = 'date',
  priority = 'priority'
}
export enum SortDir {
  asc = 'asc',
  desc = 'desc'
}

export enum ViewSize {
  collapsed = 'collapsed',
  expand = 'expand',
  none = 'none'
}